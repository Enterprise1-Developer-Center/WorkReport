<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@id/root_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >
  <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:layout_marginLeft="@dimen/dp_10"
      android:layout_marginRight="@dimen/dp_10"
      android:orientation="vertical"
      >
    <EditText
        android:id="@id/holiday_edittext"
        android:clickable="true"
        android:drawableLeft="@drawable/ic_today"
        android:focusable="false"
        android:hint="@string/date"
        android:inputType="date"
        style="@style/AddProjectEditText"
        />
    <EditText
        android:id="@id/holiday_name_edittext"
        android:clickable="true"
        android:drawableLeft="@drawable/ic_nature_people"
        android:focusable="false"
        android:hint="@string/legal_holiday"
        android:inputType="text"
        android:textColor="@android:color/holo_red_dark"
        style="@style/AddProjectEditText"
        />
  </LinearLayout>

</FrameLayout>